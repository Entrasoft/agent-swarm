# Run: python3 -m swarm_lab.credentials setup
# The hidden prompt creates an ignored .env with owner-only permissions.
# OPENAI_API_KEY in the process environment takes precedence over .env.
# Never commit a real value or paste it into chat.
# OPENAI_API_KEY=
