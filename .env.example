# Offline commands require no credentials and do not load this file.
# Live mode reads OPENAI_API_KEY from the process environment only.
# Supply it through your shell/secret manager. Never commit real values.
# OPENAI_API_KEY=
